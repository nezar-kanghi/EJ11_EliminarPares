En este ejercicio creamos una lista ArrayList de numeros enteros y eliminamos todos los numeros ares utilizando un Iterador, primero añadimos los numeros del 1 al 6 (tener en cuenta que podriamos usar un bucle while como en los siguientes ejercicios para poner los nuemros que queramos) luego recorre la coleccion con iterator() mientras haya elementos comprueba si cada numero es par usando el operador %2. Si el numero es par lo elimina correctamente usando it.remove() y asi evitar errores al modificar la lista durante el recorrido.
